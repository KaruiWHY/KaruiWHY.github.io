## Welcome

This is a small demo post written in **Markdown** with inline math like $\(e^{i\\pi} + 1 = 0\) $ and a block equation:

$$
\\nabla_\\theta \\mathcal{L}(\\theta)
$$

You can edit this file or add more `.md` files under the `posts/` folder. Each entry in `posts/posts.json` can point to a Markdown file using the `mdPath` field.

